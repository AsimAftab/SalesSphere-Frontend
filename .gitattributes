# Enforce consistent casing across all platforms
* text=auto eol=lf

# TypeScript/JavaScript files
*.ts text diff=typescript
*.tsx text diff=typescript
*.js text diff=javascript
*.jsx text diff=javascript

# Handle case sensitivity - prevents duplicate files with different casing
# This ensures all developers use the same casing regardless of OS
* text=auto
